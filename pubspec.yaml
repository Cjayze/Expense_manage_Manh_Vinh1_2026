name: quanlychitieu_manh_vinh_1_2026
description: "A new Flutter project for expense management."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.9.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.5.0

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  intl: ^0.19.0
  fl_chart: ^1.2.0
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^5.4.4
  mocktail: ^1.0.4
  fake_cloud_firestore: ^4.0.0
  hive_test: ^1.0.1

  hive_generator: ^2.0.1
  build_runner: ^2.4.8


  image_picker: ^1.1.2
  firebase_storage: ^13.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/avatars/